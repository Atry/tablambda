# Everything in this directory is machine-generated from the tablambda
# DEFUN (defunctionalization) compiler; do not edit by hand. Regenerate with
# (console scripts, or python -m):
#   compiler-examples.tex:
#     tablambda-defun-examples    (python -m tablambda_examples._examples)
#   defun-benchmark-<tag>.tex (one per interpreter; results differ markedly across them):
#     py311 (CPython 3.11), pypy (PyPy 3.11). The full benchmark is mandatory, including
#     the bootstrap row (DEFUN on DEFUN), whose committed input artifact exists only for
#     these two interpreters; on 3.12+ the benchmark fails loudly rather than emitting a
#     partial fragment, so only these two fragments exist. Regenerate one reproducibly
#     with nix run .#regen-defun-benchmark-<tag> (py311 or pypy), or the running
#     interpreter's with tablambda-defun-benchmark (python -m tablambda_examples._benchmark).
#     The paper (tab:defun) inputs the py311 fragment.
# The pre-compiled defunctionalized artifacts the benchmark imports are committed
# Python under packages/tablambda-examples/src/tablambda_examples/_generated/.
#
# self-compilation-benchmark.tex is a FROZEN legacy artifact: it was generated by
# the removed old compiler (tablambda._benchmark) and is no longer regenerable.
# It survives only as an input to the outdated legacy-draft.tex; its successor for
# the DEFUN implementation is defun-benchmark-<tag>.tex.
* linguist-generated=true
